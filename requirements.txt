# Wi-Fi CSI Human Position Sensing — Python dependencies (Mac)
# Install:  python3 -m venv .venv && source .venv/bin/activate
#           pip install -r requirements.txt

pyserial>=3.5
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.3
matplotlib>=3.8
PyYAML>=6.0
joblib>=1.3
PyQt6>=6.6

# Optional — Model B (neural network). The KNN / Random Forest baselines
# work without it; train.py skips the NN with a warning when missing.
# CPU-only wheel is sufficient for this project.
#   pip install torch
# torch>=2.2
